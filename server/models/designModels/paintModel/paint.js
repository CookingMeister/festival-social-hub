/*COMING SOON*/
/*import mongoose from 'mongoose';

const { Schema } = mongoose;

const paintJSAISchema = new Schema({
  user: { type: Schema.Types.ObjectId, ref: 'User', required: true },
  originalImage: { type: String, required: true }, // URL of the original painted image
  generatedImage: { type: String }, // URL of the AI-generated image
  description: { type: String }, // Description provided by the user
  visibility: { type: String, enum: ['public', 'private'], default: 'public' }
});

const PaintJSAI = mongoose.model('PaintJSAI', paintJSAISchema);

export default PaintJSAI;*/